/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'content.dropboxapi.com',
      'dl.dropboxusercontent.com',
      'www.dropbox.com'
    ],
  },
}

module.exports = nextConfig 